{
  "name": "klerosboardsubscriptions",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ salgozino/klerosboardsubscriptions",
    "create-local": "graph create --node http://localhost:8020/ salgozino/klerosboardsubscriptions",
    "remove-local": "graph remove --node http://localhost:8020/ salgozino/klerosboardsubscriptions",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 salgozino/klerosboardsubscriptions"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
